<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhy.mapper.TypeMapper">


    <!--按文章数量查询前几个分类-->
    <resultMap id="typequery" type="com.lhy.pojo.TypeQuery">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="blogNum" column="num"/>
    </resultMap>

    <select id="getNumType" resultMap="typequery">
        SELECT t.id id,t.name name,count(*) num
        FROM blog b , type t
        WHERE b.type_id = t.id
        GROUP BY type_id
        LIMIT #{size};
    </select>
    <select id="getNumType1" resultMap="typequery">
        SELECT t.id id,t.name name,count(*) num
        FROM blog b , type t
        WHERE b.type_id = t.id
        GROUP BY type_id

    </select>

    <!--新增保存分类-->
    <insert id="saveType" parameterType="com.lhy.pojo.Type">
        insert into blog.type values (#{id},#{name});
    </insert>

    <!--根据id，name查询分类-->
    <select id="getType" resultType="com.lhy.pojo.Type">
        select id,name from blog.type where id = #{id}
    </select>

    <!--查询所有分类-->
    <select id="getAllType" resultType="com.lhy.pojo.Type">
        select * from blog.type
    </select>


    <resultMap id="type" type="com.lhy.pojo.Type">

        <id property="id" column="tid"/>

        <result property="name" column="name"/>
        <collection property="blogs" ofType="com.lhy.pojo.Blog">
            <id property="id" column="bid"/>
            <result property="title" column="title"/>
            <result property="typeId" column="type_id"/>
        </collection>
    </resultMap>

    <!--分类页面查询-->
    <select id="getAllTypeAndBlog" resultMap="type">
        select t.id tid, t.name, b.id bid, b.title,b.type_id from blog.type t,blog.blog b where t.id = b.type_id
    </select>


    <!--根据分类名称来查询-->
    <select id="getTypeByName" resultType="com.lhy.pojo.Type">
        select * from blog.type where name = #{name}
    </select>

    <!--编辑修改分类-->
    <update id="updateType" parameterType="com.lhy.pojo.Type">
        update blog.type set name = #{name} where id = #{id};
    </update>

    <!--删除分类-->
    <delete id="deleteType" >
        delete from blog.type where id = #{id}
    </delete>


</mapper>

<!---->